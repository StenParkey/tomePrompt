const diceBox = {
	_diceTypes: [4, 6, 8, 10, 12, 20, 100],

	get diceTypes() {
		return this._diceTypes;
	},

	rollDice(diceType, amountOfRolls) {
		// Basic method for rolling a dice a certain amount of times
		return;
	}
}

export default async function diceRoll(user) {

}
